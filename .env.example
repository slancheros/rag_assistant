# Docker Compose runs Ollama by default. Override these values
# only when you want different local models or tuning.
APP_PORT=8000

LLM_MODEL=qwen3:4b
EMBEDDING_MODEL=embeddinggemma

RELEVANCE_THRESHOLD=0.35
TOP_K=2

PROVIDER_TIMEOUT_SECONDS=120
LOG_LEVEL=INFO
